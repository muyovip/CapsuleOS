[package]
name = "rewrite_tx"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ciborium = "0.2"
sha2 = "0.10"
hex = "0.4"
thiserror = "1.0"
parking_lot = "0.12"
glyph_engine = { path = "../glyph_engine" }

[dev-dependencies]
proptest = "1.4"
