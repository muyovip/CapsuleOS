[package]
name = "sonus_capsule"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ciborium = "0.2"
sha2 = "0.10"
anyhow = "1.0"
thiserror = "1.0"

[dev-dependencies]
approx = "0.5"
